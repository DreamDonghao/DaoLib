# TODO
记录项目开发中的想法和计划
## 批处理渲染器
 - 使用顶点池，每帧渲染对象直接按渲染顺序写在顶点池中。
## 使用 json 解析纹理数据
## 批处理器配套常用渲染组件：
 ### 基础渲染对象
  - 纹理 ：矩形，支持每个顶点颜色，透明度，旋转
  - 几何图形，N边形，支持每个顶点自定义颜色，每个顶点独立坐标
  - 字符，矩形，支持每个顶点颜色，透明度，旋转
## 并发处理更新与渲染